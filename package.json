{
  "name": "pet-shop-v2",
  "version": "1.0.0",
  "description": "",
  "main": "src/js/app.tsx",
  "scripts": {
    "build": "tsc && esbuild src/js/app.tsx --bundle --sourcemap --outdir=src/built",
    "watch": "npm run build -- --watch",
    "dev": "npm run build -- --serve=8080 --servedir=src/",
    "clean": "rm -rf src/built",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "esbuild": "^0.14.12",
    "ethers": "^5.5.3",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "@types/node": "^17.0.10",
    "@types/react": "^17.0.38",
    "@types/react-dom": "^17.0.11",
    "hardhat": "^2.8.3",
    "typescript": "^4.5.5"
  }
}
